spring:
  application:
    name: comment-app-micriservice
---

spring:
#  config:
#    activate:
#      on-profile: dev
#    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8888
      enabled: true